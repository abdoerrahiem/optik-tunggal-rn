const config = {
    screens: {
        Home: {
            path: "Home"
        },
        Transaction: "TransactionList"
    }
}

const linking = {
    prefixes: ["optiktunggal://app"],
    config
}

export default linking;